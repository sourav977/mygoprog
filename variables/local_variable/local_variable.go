package main

//Local Variables(Declared Inside a block or a function)
//not accessible outside the function or block.
//variables can also be declared inside the for, while statement etc. inside a function and can be
//accessed by the nested code blocks inside a function
//compile-time error if these variables are declared twice with the same name in the same scope
//variables don’t exist after the function’s execution is over.
//ariable which is declared outside the loop is also accessible within the nested loops
//variable which is declared inside a loop body will not be visible to the outside of loop body.

func main() {

}
